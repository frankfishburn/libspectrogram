language: cpp

compiler:
     - gcc

addons:
    apt:
        sources: ubuntu-toolchain-r-test
        packages: [gcc-7 g++-7 libfftw3-dev]

script:
     - export CC=gcc-7
     - export CXX=g++-7
     - mkdir -p build
     - cd build
     - cmake .. 
     - make
     - make test
